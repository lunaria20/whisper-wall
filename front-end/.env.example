# === FRONTEND-ONLY ENVIRONMENT VARIABLES ===
# These are the ONLY variables the frontend needs.
# Database credentials, secret keys, etc. belong on the backend only.

# Local Development - API Server
REACT_APP_API_URL=http://localhost:8080/api

# Production (Render) - Uncomment to override
# REACT_APP_API_URL=https://whisper-wall-backend.onrender.com/api

# Supabase Configuration (Optional)
# For direct image storage to Supabase. If not set, images store as base64.
# Use the ANON KEY only (public, safe for frontend)
REACT_APP_SUPABASE_URL=https://mqssfwzsisxwpdiujiwl.supabase.co
REACT_APP_SUPABASE_ANON_KEY=sb_publishable_KhZyeNKn6Kg2R9Mk8gx43Q_JcLeBYkg

# === BACKEND-ONLY VARIABLES (NOT on frontend) ===
# These should NEVER appear in frontend code or env vars
# DB_URL, DB_PASSWORD, SUPABASE_SECRET_KEY, etc. belong on backend only
