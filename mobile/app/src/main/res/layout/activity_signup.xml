<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen_soft"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
            android:paddingTop="72dp"
        android:paddingBottom="40dp">

        <!-- Logo Circle -->
        <ImageView
            android:layout_width="124dp"
            android:layout_height="124dp"
            android:src="@mipmap/whisperwall_logo"
            android:contentDescription="WhisperWall Logo" />

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Join WhisperWall"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="#6E2FF3" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="36dp"
            android:text="Create your anonymous account"
            android:textSize="15sp"
            android:textColor="#5E6578" />

        <!-- Sign Up Card -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_card"
            android:padding="20dp">

            <!-- Username Input -->
            <EditText
                android:id="@+id/etUsername"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:hint="Choose a username (3-20 characters)"
                android:textSize="16sp"
                android:textColor="#2F3446"
                android:textColorHint="#6D7385"
                android:background="@drawable/bg_input_field"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:inputType="text"
                android:singleLine="true"
                android:layout_marginBottom="16dp" />

            <!-- Email Input -->
            <EditText
                android:id="@+id/etEmail"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:hint="your@email.com"
                android:textSize="16sp"
                android:textColor="#2F3446"
                android:textColorHint="#6D7385"
                android:background="@drawable/bg_input_field"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:inputType="textEmailAddress"
                android:singleLine="true"
                android:layout_marginBottom="16dp" />

            <!-- Password Input -->
            <EditText
                android:id="@+id/etPassword"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:hint="8+ chars, with uppercase and number"
                android:textSize="16sp"
                android:textColor="#2F3446"
                android:textColorHint="#6D7385"
                android:background="@drawable/bg_input_field"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:inputType="textPassword"
                android:singleLine="true"
                android:layout_marginBottom="16dp" />

            <!-- Confirm Password Input -->
            <EditText
                android:id="@+id/etConfirmPassword"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:hint="Re-enter password"
                android:textSize="16sp"
                android:textColor="#2F3446"
                android:textColorHint="#6D7385"
                android:background="@drawable/bg_input_field"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:inputType="textPassword"
                android:singleLine="true"
                android:layout_marginBottom="12dp" />

            <!-- Error Message (hidden by default) -->
            <TextView
                android:id="@+id/tvErrorMessage"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textSize="13sp"
                android:textColor="#E53935"
                android:gravity="center"
                android:visibility="gone"
                android:layout_marginBottom="10dp" />

            <!-- Create Account Button -->
            <Button
                android:id="@+id/btnCreateAccount"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Create Account"
                android:textSize="16sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:background="@drawable/bg_button_purple"
                android:layout_marginTop="4dp"
                android:stateListAnimator="@null" />

        </LinearLayout>

        <!-- Anonymous note -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="4dp"
            android:text="Your identity will remain anonymous when posting confessions"
            android:textSize="12sp"
            android:textColor="#AAAAAA"
            android:gravity="center"
            android:textAlignment="center" />

        <!-- Already have account -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Already have an account? "
                android:textSize="14sp"
                android:textColor="#5E6578" />

            <TextView
                android:id="@+id/tvLogin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Log In"
                android:textSize="14sp"
                android:textColor="#6E2FF3"
                android:textStyle="bold"
                android:clickable="true"
                android:focusable="true" />

        </LinearLayout>

    </LinearLayout>
</ScrollView>